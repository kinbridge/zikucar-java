<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.think.mapper.SysUserMapper">
    <sql id="Base_Column_List">
        pk_id, dept_id, position_id, uin, username, pwd, age, email, phone, `status`, create_date,
    create_by, update_by, update_date, remark
    </sql>

    <update id="editUserStatus" parameterType="com.think.entity.SysUser">
        update t_sys_user t set t.status=#{status} where t.pk_id=#{pkId}
    </update>
</mapper>